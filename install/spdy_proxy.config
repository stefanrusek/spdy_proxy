% spdy_version - valid values are 2, 3, or negotiate
{spdy_version, negotiate}.

% listen - the port to accept SPDY connection on
{listen, 9999}.

% server, port - the server and port to forward requests to
{server, "localhost"}.
{port, 80}.
